name: Test

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted,linux]
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: printf '4\n1\nn\ny\nn' | bash nrchkb-ffmpeg-build.sh
