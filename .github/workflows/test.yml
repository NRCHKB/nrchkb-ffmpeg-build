name: Test

on:
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted,linux]
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: bash nrchkb-ffmpeg-build.sh
